Journal Phase 2
When I tried to log defaultTaxRate outside of the function, I got an error because the variable was only available inside the calculateStoreTax function. This is because defaultTaxRate has local scope, meaning it can only be accessed within the function where it was created.
