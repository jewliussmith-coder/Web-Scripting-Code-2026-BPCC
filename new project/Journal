Journal Phase 5
Breaking the order process into small, single-purpose functions made calculateFinalBill easier to write because each function already handled one specific part of the calculation. Instead of putting all the math into one long block of code, I could call each function step-by-step to calculate the subtotal, discount, tax, and final total.
