Journal: 

Using .pop() removed the last object from the receiptItems array. When I recalculated the subtotal by looping through the array again, the removed item was no longer included, so the subtotal and final bill automatically changed. This makes it easy to modify the order because the calculations are based on the current contents of the array.
